# Changelog

## [0.5.0] - 2026-07-06

Consolidation release. One calibrator path, one estimator, one ingestion layer, a flattened
interface with typed results, and slimmer packaging. The README quickstart output is
byte-identical to 0.4.3, and all estimates and standard errors on the default paths are
unchanged to float equality. (Combines the 0.5.0 work merged as PRs #28-#31 plus this
release PR.)

### Breaking

- **`result.best_policy()` returns a gate-aware `PolicyVerdict`** (name, index, estimate,
  flagged, all_flagged, runner_up) instead of a naive argmax `int`. A flagged argmax is
  demoted (recorded as `runner_up`) and the best reliable policy wins; if everything is
  flagged, the argmax returns with `all_flagged=True`. Use `verdict.index` for the old
  value. The CLI's best-policy rendering consumes this verdict.
- **`estimator_config` is validated**: unknown keys raise a ValueError listing the valid
  keys (`oua_jackknife`, `inference_method`, `n_bootstrap`, `bootstrap_seed`,
  `use_augmented_estimator`, `paired_comparison`); a user `oua_jackknife` now OVERRIDES
  the pipeline default instead of raising a duplicate-keyword TypeError; passing
  `reward_calibrator` raises "reward_calibrator is managed by analyze_dataset".
- **Calibration files must be in [0, 1]**: `calibration_data_path` values outside [0, 1]
  raise a hard error naming the observed range instead of being silently filtered.
  Rescale the file, or pass the data in-memory via `fresh_draws_data` / on disk via
  `fresh_draws_dir` (both auto-normalize any bounded scale).
- **`fit_cv` without `prompt_ids`** now synthesizes stable row-index ids and uses the
  canonical hash-fold path (the legacy KFold+blake2b fallback is deleted), so fold
  assignments change for prompt-id-less direct `fit_cv` calls.
- **The undocumented `{policy}_fresh.jsonl` file pattern is dropped** — use
  `{policy}_responses.jsonl` or `{policy}.jsonl` (see `cje.data.ingest.POLICY_FILE_PATTERNS`).
- **Packaging**: the wheel no longer ships `cje.tests`; `pandas` is no longer a dependency;
  `seaborn` is no longer installed by the `viz`/`all` extras (they now pull matplotlib only).
- **Removed names** (tombstones raise ImportError/ValueError pointing at the replacement):

| Removed | Replacement / rationale |
|---|---|
| `BaseCJEEstimator` (`cje.estimators`, `cje.advanced`) | Merged into `CalibratedDirectEstimator` — its only subclass. Also gone: `get_influence_functions`, `diagnostic_config`, the `run_diagnostics` knob (diagnostics are always built). |
| `AnalysisService`, `AnalysisConfig`, `cje.interface.factory` (`create_estimator`, `get_estimator_names`, `REGISTRY`, `BuilderFn`) | Call `analyze_dataset` — it owns the whole Direct-mode pipeline as module-level functions in `cje/interface/analysis.py`. Estimator-name validation lives in `cje/interface/_removed.py`; every 0.4.0 migration-error message is unchanged verbatim. |
| `calibrate_from_raw_data`, `calibrate_judge_scores` (`cje.calibration`, `cje.advanced`) | `calibrate_dataset`, `JudgeCalibrator.fit_cv`, or `cje.calibrated_mean_ci`. |
| `JudgeCalibrator.fit_transform`, `.predict_all`, `.index`, `.has_fold_models`; `FlexibleCalibrator.index`, `.get_diagnostics`, `.iso_reg` | `fit_cv` is the only fitting path (cross-fitted). |
| `calibrate_dataset(enable_cross_fit=False)` | Raises — cross-fitted calibration is the only mode. |
| `CalibratedDirectEstimator` params `calibration_data_path` (inert TODO stub), `calibration_policy`, `use_multipolicy_eif` shim; the `"analytical"` inference alias | Transport experiments call `cluster_bootstrap_direct_with_refit(calibration_policy_idx=...)` directly; use `inference="cluster_robust"`. |
| `cje.diagnostics.compare_policies_bootstrap` | Half-finished, zero production callers — `EstimationResult.compare_policies(i, j)`, or paired contrasts from the returned `bootstrap_matrix`. |
| `plot_calibration_comparison` (`cje`, `cje.visualization`, `cje.advanced`; whole `visualization/calibration.py`) | Removed; calibration quality lives in `results.diagnostics` (`calibration_rmse`, `calibration_coverage`). |
| `EstimationResult.plan_allocation` | `fit_variance_model` + `plan_evaluation`. |
| `cje.simulate_planning_sweep` (also from `cje.diagnostics`) | Loop `simulate_planning` yourself. |
| `cje.utils.export.export_results_csv`, `cje.utils.aggregate_diagnostics`, `cje.utils.analyze_diagnostics`, `cje.utils` matplotlib re-exports | JSON export (`export_results_json`) is the canonical serialized form; plots live in `cje.visualization`. |
| `cje.diagnostics.display.format_diagnostic_comparison`, `DirectDiagnostics.to_csv_row`, `DirectDiagnostics.from_dict` | Unused display/serialization surface. |
| `Sample.base_policy_logprob`, `Sample.target_policy_logprobs` (+ validators), `Sample.get_importance_weight`, `LogProbStatus`, `LogProbResult` | OPE-era schema. Logprob fields in input FILES are still accepted-and-ignored; the data model simply no longer carries them (no more fabricated dummy logprobs internally). |
| `Dataset.validate_policies_exist`, `Dataset.filter_valid_samples`, `Dataset.summary` | Dead data-layer surface. |
| `DatasetFactory` / `DataSource` / `InMemoryDataSource`, `reward_utils.py` | `load_dataset_from_jsonl` calls `DatasetLoader` directly. |
| `save_fresh_draws_to_jsonl`, `FreshDrawSample.fold_id`, dead fold helpers, per-prompt `FreshDrawDataset` helpers | Unused; `draws_per_prompt` is now a computed property. |
| `BoundaryCard.estimator_gap` (+ `boundary_card`'s `est_calips`/`est_dr` params) | Never populated by the 0.4.x pipeline; serialized cards no longer carry an always-None key. |

Note: `IPSDiagnostics` — which the 0.4.0 notes said would be removed in 0.5.0 — is RETAINED
as a deprecated alias of `DirectDiagnostics` and is now slated for a future release.

### Fixed

- **`estimator="direct"` vs `"calibrated-direct"`**: the names are aliases of the one
  calibrated estimator. The CLI's pre-mapping of the name is deleted so
  `metadata["estimator"]` records the same resolved name from both surfaces, and the docs
  no longer claim `"direct"` skips calibration (whether calibration runs is driven solely
  by oracle-label availability).
- **CAUTION boundary cards now reach the status ladder**: a CAUTION card yields
  `Status.WARNING` for that policy (previously unreachable — CAUTION left the policy
  GOOD); WARNING does not flag the policy or refuse level claims.
- **`predict_oof` raises on fold ids with no fitted model in every mode** (previously only
  monotone raised; flexible modes silently fell back to the full model), and
  `get_oof_predictions` requires the fold ids from the `fit_cv` result instead of silently
  substituting full-model predictions when fold info looked wrong.
- **`build_direct_eval_table` raises the same actionable error as
  `CalibratedDirectEstimator.fit()`** when a required covariate is missing from a sample's
  metadata (it used to NaN-fill silently).
- **Bare `{policy}.jsonl` directories now load end-to-end**: discovery, loading, and the
  CLI resolve policy files from the same pattern list, so any layout that is discovered is
  guaranteed to load.
- **0–100/Likert files on `fresh_draws_dir` no longer crash** — directory input gets the
  same joint scale detection + `NormalizationInfo` as in-memory data (verified
  dir-vs-dict bit-identity on a 0–100 scale).
- **`confidence_interval()` is raise-free on both inference paths** (consults the typed
  `ci_info` first, falling back to metadata sniffing for older results); requesting a
  different `alpha` than the stored bootstrap alpha logs a warning on both paths.
- **`import cje.advanced` no longer loads matplotlib** (plot names resolve lazily);
  `cje.visualization._require_viz(name)` is the single source of the
  `pip install "cje-eval[viz]"` ImportError hint.
- **`plan_evaluation`/`plan_for_mde` log a warning when planning on a variance model with
  `fit_ok` False** (previously the only guard was a verbose-mode print).
- **`EstimationResult.to_dict()` no longer clobbers diagnostics**, and stale IPS/DR and
  teacher-forcing error strings were replaced with Direct-mode wording.
- **Docs truth pass**: subpackage READMEs, PLAYBOOK.md, and the agent skill scrubbed of
  every removed name; label-tier docs now match measured behavior (0 labels → loud
  `naive_direct` fallback; 1–3 → hard error; 4–9 → fold auto-reduction with noisier CIs);
  `compare_policies` gained its first tests and a docstring note that its
  influence-function z-test SE basis differs from the headline bootstrap SEs.

### Added

- **Typed results on `EstimationResult`** (metadata mirrors keep being written and stay
  the serialized source of truth): `result.summary()` (compact text report — the `cje`
  package docstring example runs as written), `result.best_policy()` → `PolicyVerdict`,
  `result.gates` → `Dict[str, GateResult]`, `result.target_policies`, and `result.ci_info`
  (`CIInfo`, written by the estimator on both inference paths).
- **`cje.data.ingest`**: `POLICY_FILE_PATTERNS`, `resolve_policy_file`,
  `read_aliased_field`, `resolve_prompt_id`, `read_jsonl_records`,
  `fresh_draws_data_from_file` — one file-pattern resolver shared by discovery, loading,
  and the CLI. `fresh_draws_dir` also accepts a single multi-policy JSONL file path.
- **`cje.calibration.resolve_n_folds(n_oracle, requested_folds)`**: the 4–9-label fold
  auto-reduction moved from `analyze_dataset`'s service layer into `fit_cv`, so every
  caller gets it — e.g. `calibrated_mean_ci` with 8 labels now succeeds (with the
  fold-reduction warning) instead of raising.
- **Shared inference recipes in `cje.diagnostics.robust_inference`**:
  `oracle_jackknife_estimates` (the leave-one-oracle-fold loop),
  `combine_cluster_and_oracle` (the SE/df combining rule), and `oracle_jackknife_variance`
  (moved from the estimator base class) — previously triplicated across the base
  estimator, the direct estimator, and the array API. Plus
  `cje.diagnostics.boundary_card_dict` (card + serialization + REFUSE-LEVEL warning,
  shared by the estimator and `calibrated_mean_ci`).
- **Planning polish**: `simulate_planning` gained `m_min` (forwarded to `plan_evaluation`;
  default 30, matching); `FittedVarianceModel.fit_ok` property (`r_squared >= 0.5`).
- **0.5.0 tombstones**: the highest-traffic removed names (`BaseCJEEstimator`,
  `calibrate_from_raw_data`/`calibrate_judge_scores`, `plot_calibration_comparison`,
  `compare_policies_bootstrap`) raise informative ImportErrors naming the replacement;
  all 0.4.0 tombstones keep working (pinned by tests).
- **CI**: Python 3.11 added to the test matrix (now 3.9–3.13 complete).
- **Docs**: "Migrating from 0.4.x" section in the agent-skill reference; typed-results
  documentation in the data and interface READMEs; canonical status-ladder mappings
  documented in the diagnostics README.

### Changed

- **One cross-fitted isotonic implementation**: `JudgeCalibrator` delegates every mode
  (monotone / two_stage / auto) to one `FlexibleCalibrator`; the two previous
  implementations are numerically equivalent for oracle labels in [0, 1] (which every
  supported entry point guarantees). `predict_oof(scores, fold_ids, covariates)` is the
  single OOF entry point.
- **Normalization on ALL input paths**: `fresh_draws_dir` routes through
  `fresh_draws_from_dict`, so directory input gets the same auto-normalization as
  in-memory data; "any bounded scale" now holds everywhere fresh draws enter.
- **Hard errors replace silent filters**: out-of-range calibration files raise (see
  Breaking); loud-by-default hardening throughout the eval-table and OOF paths (see Fixed).
- **Status ladder**: canonical mappings in `cje.diagnostics.gates` —
  `BOUNDARY_CARD_STATUS_TO_STATUS` ({OK: GOOD, CAUTION: WARNING, REFUSE-LEVEL: CRITICAL})
  and `TRANSPORT_STATUS_TO_STATUS` ({PASS: GOOD, WARN: WARNING, FAIL: CRITICAL});
  `SATURATION_CAUTION_THRESHOLD` (0.20) and `TRANSPORT_FAIL_DELTA_THRESHOLD` (0.05) moved
  to `gates.py`.
- **`CalibratedDirectEstimator.fit()` vectorized**: one `predict` call per policy instead
  of per sample (numerically identical; large speedup for big eval sets).
- **Viz layout**: `TransportDiagnostics.plot` and `plot_transport_comparison` moved to
  `cje.visualization.transport` (matplotlib-free until called);
  `from cje.diagnostics import plot_transport_comparison` still works via a lazy re-export.
- **Packaging/tooling**: version 0.5.0; `pandas` removed from dependencies (zero imports
  in the package or tests); `seaborn` removed from the `viz`/`all` extras (zero imports
  anywhere); `watchdog` and `types-PyYAML` removed from dev dependencies (and
  `types-PyYAML` from the pre-commit mypy hook); tests excluded from the wheel
  (`exclude = ["cje/tests", "cje/tests/**/*"]`); `black` dev dependency bumped to `^25.1`
  (matching the pre-commit rev); `setup.py` (advertised a wrong package name/email),
  `MANIFEST.in` (ignored by poetry-core), and `.python-version` (personal pyenv name)
  deleted; `make lint` runs `black --check` (no longer mutates the tree); mypy excludes
  pruned to existing paths; slow-test markers added to the ~27s arena-covariates test and
  eight ~4.5s integration tests, cutting the fast-suite runtime roughly in half.

## 0.4.3

Fixes from a two-agent statistical review of the planning/power code. The core algebra (square-root allocation law, MDE formula, measurement-grid design) was verified correct and is unchanged; these fixes target the constrained/degenerate paths, budget accounting, and the simulation path.

- **Fix `plan_for_mde` missing the target when the m_min floor binds**: the closed-form budget inversion is exact only at the unconstrained optimum. When the implied unconstrained m* fell below `m_min`, the returned plan's MDE could overshoot the target by ~75% while understating the required budget (e.g. target 0.011 → plan MDE 0.0193 at $505 instead of the correct ~$594). The binding case now fixes m = m_min and solves for the n that delivers the residual variance, and every returned plan is verified to achieve `plan.mde ≤ 1.02 × target_mde` (budget bumped minimally on integer-rounding overshoot).
- **Fix budget overrun when `sigma2_cal == 0` exactly**: that branch let n consume the entire budget and then added the m_min oracle cost on top ($5,480 spent on a $5,000 budget). n is now funded from the budget net of the oracle floor. The mirrored `sigma2_eval == 0` and near-degenerate (denom ≈ 0) branches were made symmetric, and `plan_evaluation` now enforces `total_cost ≤ budget` as a hard postcondition.
- **Raise ValueError on infeasible budgets**: when the budget cannot fund the cheapest valid plan (`m_min` oracle labels plus `m_min` surrogate scores, since n ≥ m ≥ m_min), `plan_evaluation` raises with the floor cost stated instead of silently returning a plan costing up to 4.8× the budget. The planning dashboard's cost-sensitivity panel now plots only the feasible portion of each scaled-cost curve accordingly.
- **Refuse degenerate variance models**: `plan_evaluation` raises ValueError when both variance components are zero, and `EvaluationPlan.power_to_detect` raises ValueError (instead of ZeroDivisionError) on a zero-SE plan.
- **Fix `fit_variance_model` recording requested instead of actual (n, m)**: `_measure_variance_direct` clamps allocations when the unlabeled pool is too small for n − m, but the fit recorded the requested sizes. Measurements now use the actual sizes, with a single consolidated warning naming the cause and the number of affected cells.
- **Unify `simulate_variance_model` with the pilot-based path**: the simulation used the pre-0.4.0 pool-based m-grid (at `oracle_fraction=1.0` it recorded (n, m) pairs `analyze_dataset` never actually received) and a copy-pasted NNLS fitter. It now derives the measurement grid via the same shared helper as `fit_variance_model`, supplies judge-only records by stripping labels from the remaining pool (valid because synthetic labeling is random), and calls the shared `_fit_variance_model_from_measurements`.
- **Calibrate the synthetic generator to deliver the requested isotonic R²**: the mixing weight targeted the continuous oracle while labels are binarized at 0.5, attenuating the realized isotonic R² 15–25% below the request (request 0.5 → realized 0.39). The internal mixing weight is now calibrated by bisection against the binarized labels on a large seeded probe sample (realized R² within ±0.02 of the request, deterministic given seed). The binary output contract is unchanged.
- **Disclose two systematic conservatisms**: plan summaries, the planning docstrings, and PLAYBOOK.md now state that MDE assumes independent policies (paired evals on a shared prompt set typically detect smaller differences) and that variance components measured from bootstrap SEs are conservative at small oracle counts — treat planned budgets as upper bounds.
- **Docstring corrections**: `correlation_to_r2(0.7, "monotone")` example corrected to 0.745 (was 0.735) and the "conservative" claim reworded — the midpoint-to-1 heuristic inflates R² for truly linear relationships. Stale sweep runtime estimates ("15–21 minutes") replaced with measured ~25–30 s per R² value, and the synthetic generator's `Var(judge|oracle)` math note fixed (off by the uniform-variance factor 1/12).

## 0.4.2

Docs-only release so the improved quickstart renders on PyPI. No code changes.

- **Quickstart shows partial oracle coverage on the base policy**: the previous example labeled all 10 of `gpt-5.6`'s rows, implying the policy that carries the oracle slice needs complete coverage. It now has 20 responses with labels on only 10 — records with `oracle_label=None` are treated as unlabeled — while `fable-5` remains fully judge-only. The calibrator is fit on whichever rows have labels, pooled across policies; labels are a pooled budget, not a per-row requirement. Output block re-shot from a real run (byte-exact).

## 0.4.1

Docs and UX patch from the post-release review. No estimation behavior changes except the transport-audit CI critical values, two log levels, and one error message.

- **Redesigned the README quickstart around calibration transfer**: the old example labeled 6/8 rows of BOTH policies — teaching the label-under-every-policy anti-pattern CJE exists to remove. The quickstart now shows one policy carrying a 10-label oracle slice while the second policy is judge-only and still gets a calibrated estimate with an honest CI; the interface README's in-memory example matches. Output blocks are pasted from real runs (byte-exact).
- **Fixed the HealthBench link 404** (`/blog/` → `/research/`), absolutized the README's relative image sources and doc links so they render on PyPI, and pointed the `Documentation` project URL at https://cimolabs.com/cje instead of the arXiv paper.
- **Corrected the "t-based CIs" overclaim** in the interface, estimators, and data READMEs: CIs are percentile bootstrap by default; t-based with finite-sample df only under `cluster_robust` inference (the `degrees_of_freedom` metadata only exists there).
- **Documented the 0.3.x escape hatch's Python ceiling**: every "pin `cje-eval==0.3.*`" pointer (README, `analyze_dataset` and estimator migration errors) now notes it requires Python <=3.12 — on 3.13 use a 3.12 env for OPE. The verbatim migration-error pins in `test_migration_errors.py` are updated to match.
- **Docs accuracy sweep**: estimators README no longer claims NaN estimates for small oracle slices (actual behavior: 4–9 labels reduce the fold count with a warning; <4 raises); tests README updated to the real file tree (11 missing files added), the `>=3.9,<3.14` Python window, and real test counts; deduped the doubled 0.4.0 planning-grid bullet in this changelog; PLAYBOOK's `n_replicates=150` example is labeled slow/production-grade (default 5 runs in ~30s).
- **Notebooks install the viz extra**: `cje_core_demo.ipynb` and `cje_planning.ipynb` now `pip install "cje-eval[viz]"` — both notebooks plot, and the bare install left Colab users with an ImportError at the first chart.
- **CLI/logging polish**: the `--estimator-config` help example is now `'{"n_bootstrap": 4000}'` (the old `'{"n_folds": 10}'` crashed — no such kwarg); the too-few-oracle-labels calibration error tells you to add `oracle_label` values to more samples (>=10 pooled) instead of recommending an `n_folds` parameter the interface doesn't expose; per-replicate bootstrap calibration fits log at DEBUG instead of INFO, eliminating a ~4k-line "CV Calibration complete" stderr flood at `n_bootstrap=2000` (the one-time full-data fit logs keep INFO).
- **Transport audit CIs use t critical values** (df = n_probe − 1) instead of z (1.96) — slightly wider at the recommended probe sizes (40–60 rows), restoring the advertised 5% false-alarm rate; verdicts near the boundary may flip from FAIL to PASS. `audit_transportability` and the array-API `transport_audit` accept `alpha` (default 0.05). Docs now state that per-decile residuals are display-only (4–6 rows per bin at recommended probe sizes — never gate on them) and that K-policy audits at per-test α inflate the family-wise false-alarm rate (BH correction is a planned option, not implemented). Regression tests pin the CI to scipy's one-sample t interval and a boundary case where the z verdict flips.
- **Removed OPE-era leftovers**: `.env.example` (all five API keys were dead — the library makes no network calls), `cje/tests/data/create_test_data.py` and its five orphaned logged-data fixtures (zero references; the OpenCompass fixtures stay), and the unexported `validate_fresh_draws`/`create_synthetic_fresh_draws` helpers in `fresh_draws.py` (both consumed importance weights; zero callers). `boundary_card`'s docstrings and the diagnostics README no longer describe the optional estimator-gap signal in terms of removed estimators — it is a cross-estimator signal the 0.4.x pipeline never computes.

## 0.4.0

**BREAKING — CJE 0.4.0 is Direct-mode only.** The library now does one thing: calibrated evaluation of LLM policies from judge-scored fresh draws, with audits that refuse unsupported claims. The off-policy (IPS/DR) machinery — which requires teacher-forced logprobs no realistic user has, and which the paper's own results show mostly failing for LLM policies (target-typicality coverage 0.19–0.49 on realistic policy pairs even when ESS looked healthy; the best doubly-robust stack merely matched Direct mode's RMSE at ~12× the compute) — is removed. The core library drops from ~28.1k to ~13.5k lines. **OPE workflows: pin `pip install "cje-eval==0.3.*"`** (frozen on the `0.3.x` maintenance branch; docs and the OPE notebook live at the `v0.3.0` tag). Logged data with `judge_score` + `oracle_label` still works in 0.4.0 as the calibration source via `calibration_data_path`. Every removed entry point raises a migration error saying exactly this.

- **Removed the off-policy estimators**: `CalibratedIPS`, `DRCPOEstimator`, `MRDREstimator`, `TMLEEstimator`, `StackedDREstimator`, and the outcome models. `estimator=` names `calibrated-ips`, `raw-ips`, `dr-cpo`, `mrdr`, `tmle`, `stacked-dr` now raise a migration error pointing at the 0.3.x releases.
- **Removed `logged_data_path` and OPE mode routing**: `analyze_dataset` is Direct-only; passing `logged_data_path` raises a migration error. Logged data with judge scores and oracle labels still works for calibration via `calibration_data_path`.
- **Removed teacher forcing** (`cje.teacher_forcing`) and the `teacher-forcing` extra: no logprob computation ships with the library. Logprob fields in input data are ignored.
- **Removed `PrecomputedSampler`** and the sampler dependency in the estimator base class.
- **Removed the OPE weight/overlap diagnostics**: weight ESS/tail-index computation, TTC and CLE diagnostics, judge-space Bhattacharyya overlap gates, weight/DR dashboards, and the extreme-weights analysis. The Direct-relevant diagnostics (transportability audits, coverage badge/boundary cards, cluster bootstrap, planning) survive.
- **Removed weight-stabilization (SIMCal) and oracle-slice calibration**; judge→oracle reward calibration is unchanged.
- **Removed the Hydra entry point** and the `hydra` extra, the auto mode detection, and `cje.research`.
- **Collapsed the interface to the single Direct flow, with exact migration errors**: `AnalysisService.run()` is one pipeline (validate config → load draws → resolve calibration source → calibrate → estimate → denormalize → metadata); result metadata keeps the `mode`/`mode_selection` keys for 0.3.0-output consumers (reason: "Direct mode is the only mode in 0.4.x"). `cje analyze PATH` now takes fresh draws — a directory of per-policy response files or a single JSONL file with a `target_policy` field per record — instead of the logged-dataset positional; `--fresh-draws-dir` is kept as an alias, `--calibration-data` supplies the judge→oracle calibration source (your old logged data works there), and the default estimator is `calibrated-direct`. Logprob fields in fresh-draws inputs are ignored with an INFO note; the reliability-aware best-policy announcement is unchanged. The removed entry points fail with the full migration copy (pinned verbatim by `cje/tests/test_migration_errors.py`): `analyze_dataset(logged_data_path=...)`, the CLI's old logged positional, removed estimator names — including the 0.3.x ghost aliases `oc-dr-cpo`/`tr-cpo`/`tr-cpo-e` — and `cje.advanced.<DeletedOPEClass>` (informative `ImportError`).
- **New array-first API (`cje.array_api`)**: `calibrated_mean_ci(judge_scores, oracle_labels, ...)` is the library's documented bottom layer — a ppi_py-style primitive that takes plain numpy arrays (judge scores plus a partially-labeled oracle slice; default mask `~np.isnan(oracle_labels)`, optional boolean `oracle_mask`, `cluster_ids`, and `covariates` for two-stage calibration) and returns a `CalibratedMeanResult` (estimate, se, ci, n, n_oracle, method, the fitted calibrator, and diagnostics including the coverage badge vs the calibrator's oracle S-range). It wraps the exact `CalibratedDirectEstimator` internals with no new statistics: `inference="bootstrap"` runs the cluster bootstrap with calibrator refit (percentile CI), `"cluster_robust"` runs CRV1 SEs augmented with the oracle-jackknife variance (t-based CI), and `"auto"` follows the estimator's coupling rule (the oracle slice lives inside the evaluation sample, so it resolves to bootstrap). Equivalence with `CalibratedDirectEstimator` on the arena sample is pinned to numerical identity for both inference methods by the new `cje/tests/test_array_api.py`, alongside mask-vs-NaN equivalence, seed determinism, and loud input validation. `transport_audit(judge_scores, oracle_labels, calibrator)` is the matching array-first wrapper around `audit_transportability` (NaN probe labels dropped). Both functions and `CalibratedMeanResult` are exported from `cje`; multi-policy paired comparisons remain `analyze_dataset`'s job.
- **Boundary gate wired into Direct mode**: `JudgeCalibrator` now records its oracle-slice judge-score and reward support at fit time (`oracle_s_range`/`oracle_reward_range`), and `CalibratedDirectEstimator.estimate()` computes the paper's per-policy coverage badge (`boundary_card`) against it — 0.3.x computed boundary cards only in CalibratedIPS, so a DM-only library would have shipped no coverage badge at all. Cards are attached to `diagnostics.boundary_cards` and `result.metadata["boundary_cards"]`; at ≥ 5% out-of-range judge mass (`OUT_OF_RANGE_REFUSE_THRESHOLD`) the estimator warns loudly, sets that policy's status to CRITICAL, and flags it in `result.metadata["reliability_gates"]` (`refuse_level_claims`), so the CLI's reliability-aware trophy demotes boundary-violating winners end-to-end.
- **`DirectDiagnostics` replaces the OPE diagnostics classes**: the new slim dataclass carries estimates, per-policy statuses, coverage badges (`boundary_cards`), and calibration quality — and drops the weight fields Direct mode used to fill with hardcoded fakes (`weight_ess=1.0`, per-policy ESS/max-weight of 1.0, tail indices, Hellinger/TTC/A_B). `IPSDiagnostics` survives as a deprecated alias of `DirectDiagnostics` (removed in 0.5.0); `DRDiagnostics`, `CJEDiagnostics`, `GateState`, the ESS ladder (`ess_status` and thresholds), and the weight-summary/DR display tables are deleted with their consumers.
- **Sampler-free estimator base class**: `BaseCJEEstimator.__init__` now takes `target_policies` as its first (required) argument; the deprecated `sampler` placeholder, the importance-weight cache (`get_weights`), and the DR-estimator probe are gone. `_apply_oua_jackknife` reads oracle coverage from the reward calibrator only.
- **Direct-mode fast layer in the Monte Carlo coverage harness**: the fast (CI) layer of `test_mc_coverage.py` covered only the removed OPE estimators, which would have left 0.4.0 without its inference regression guard. It now runs `CalibratedDirectEstimator` (cluster-robust inference, R=40, n=800): point-estimate unbiasedness against the DGP's known truth, reported-SE vs empirical-SD in [0.7, 1.3] at 100% oracle coverage, a not-understated bound (≥ 0.7) at 25% coverage, and a deterministic boundary-card regression (cards appear in results and fire on out-of-range judge scores). The boundary-card cases from the retired `test_refusal_gates.py` are ported to the new `test_direct_diagnostics.py`, which also pins the `IPSDiagnostics is DirectDiagnostics` alias, the `reliability_gates`/`boundary_cards` metadata contract, and the CLI trophy demotion for a boundary-violating argmax.
- **Fixed `cje validate` false-failing ALL valid data; redesigned validation for Direct mode**: since the 0.2.x era, `cje validate` round-tripped records through the Dataset model before validating — the round-trip regenerated `prompt_id`s and relocated judge/oracle fields, so every valid file reported "Missing required field: prompt_id" / "No evaluation field found", and fresh-draws files (no `target_policy_logprobs`) crashed outright. The command now validates the RAW parsed JSONL: it accepts a fresh-draws directory (every discovered per-policy file is validated, with per-policy sample + oracle counts reported) or a single JSONL file, and exits 0 on valid data — verified on the bundled `examples/arena_sample` fresh draws. A logged-style file with judge + oracle pairs but no `target_policy` validates as a calibration source (the output says to pass it via `--calibration-data`). `validate_cje_data` is replaced by `validate_direct_data(records, judge_field, oracle_field)`, which checks raw dicts with the leading-window scan (min(100, max(10, n // 10))): `prompt_id` present, numeric judge scores (top level or metadata), oracle-label count ladders (0 or <10 labels is an issue; 10-49 an informational note) pooled across policies, and per-policy consistency when `target_policy` is present. There are NO logprob checks — logprob fields earn only the informational note "logprob fields present and ignored (Direct mode)" (entries prefixed `Note:` never affect validity).
- **Loud fresh-draws loading + an actionable viz hint**: `FreshDrawLoader.load_from_jsonl` used to skip invalid lines with a `logger.warning` nobody reads, silently shrinking the dataset — it now raises `ValueError` with file + line context (mirroring `load_fresh_draws_auto`); blank/whitespace-only lines are skipped, not errors. `import cje` no longer imports the visualization package eagerly, so the 0.3.x import-time `ImportWarning` on non-viz installs is gone; `plot_policy_estimates`/`plot_calibration_comparison`/`plot_planning_dashboard` resolve lazily via a module `__getattr__`, and accessing them without matplotlib raises `ImportError: <name> requires the viz extra. Install with: pip install "cje-eval[viz]"` instead of an `AttributeError` (same hint from `cje.visualization` and `cje.advanced`).
- **Fixed the calibration-data-only NaN gap (pre-existing) and the never-populated calibration diagnostics**: with `calibration_data_path` only and NO oracle labels in the fresh draws, the default bootstrap inference built its evaluation table from the label-free draws — the calibrator refit failed, every replicate was skipped below `min_oracle_per_replicate`, and NaN estimates came back quietly. `CalibratedDirectEstimator` now detects zero eval-oracle rows before dispatching and falls back to cluster-robust SEs with the oracle jackknife, with a loud warning and the downgrade recorded in `metadata["inference"]` (`requested_method`/`fallback_reason`). The fallback is the statistically correct SE there, not an approximation: with a separate calibration source and label-free draws, calibration and evaluation are independent, so the calibration/evaluation covariance the bootstrap exists to capture is exactly zero and the additive decomposition (cluster-robust eval variance + jackknife calibration variance) is exact. Relatedly, the 100%-oracle-coverage OUA skip no longer fires in this scenario (new `_oracle_coverage_for_oua` hook): the calibrator's coverage describes its own — typically fully labeled — calibration file, not the label-free eval set, so skipping would have silently dropped the calibration uncertainty the fallback promises; the skip still applies when the labels live in the evaluation data itself. Also fixed: `_build_diagnostics` guarded on a `get_calibration_info` method `JudgeCalibrator` never implemented, leaving `calibration_rmse`/`calibration_coverage`/`n_oracle_labels` always None — the calibrator now records its fit-time metrics (RMSE, coverage@0.1, oracle count, OOF variants after `fit_cv`) and exposes them via `get_calibration_info()`, so Direct diagnostics finally report calibration quality.
- **Minimal judge+oracle calibration files load**: the documented migration path — pass your logged data *or a minimal `{prompt_id, judge_score, oracle_label}` file* as `calibration_data_path` — used to crash for files without logprob fields (`target_policies` 'too_short' from the Dataset model; records without `prompt`/`response` skipped wholesale) even though `cje validate` blessed them as a calibration source. `DatasetLoader` now defaults the text fields to "" and `Dataset.target_policies` accepts an empty list; genuinely invalid records still fail loudly.
- **Fixed planning measurement-grid construction for large oracle pools**: `fit_variance_model` derived its calibration levels as fractions of the *full* oracle pool, so a pilot with many labels relative to `n_grid` (e.g. 480 labels with `n_grid=[100, 200]` → m ∈ {120, 240}) produced m values that exceeded most n; the `m < n` filter then collapsed the grid below 2 unique values per dimension and every planning run raised "Grid has insufficient variation" (broken since ≤0.2.25). The m levels now derive from the grid's own scale — `n_ref = min(max(n_grid), n_oracle_available)` — capped at 90% of `min(n_grid)`, so every n retains every m level and the design stays fully orthogonal. The insufficient-variation message prints sorted lists instead of raw set literals. NNLS/variance-model math untouched. The five slow planning tests pass again, plus new fast grid-construction unit tests.
- **Packaging**: version 0.4.0. Removed dependencies: `python-dotenv` (unused) and the teacher-forcing/hydra stacks (`transformers`, `fireworks-ai`, `hydra-core`, `omegaconf`) along with their extras and the orphaned `cje/conf/` Hydra estimator YAMLs (they configured deleted OPE estimators) — the only extras are now `viz` and `all`, both `[matplotlib, seaborn]`. `cje.__version__` is single-sourced (the adjacent `pyproject.toml` in source checkouts, the cje-eval distribution metadata when installed) instead of a hardcoded constant that had drifted. Python window widened to `>=3.9,<3.14` and numpy to `>=1.26,<3.0` — the fast suite is green on Python 3.13 with numpy 2.5 (no code changes needed). CI now tests a Python matrix (3.9/3.10/3.12/3.13) instead of 3.10 only. PyPI description/keywords no longer advertise off-policy estimation. Docs rewritten for the one-mode story: new root README (quickstart shows a real Direct-mode refusal; array-API section; OPE routing points at 0.3.x), rewritten module READMEs, PLAYBOOK scrubbed of OPE, `cje_core_demo.ipynb` gains a coverage-badge section, and `cje_advanced.ipynb` is a stub pointing at the `v0.3.0` tag.

## 0.3.0

Statistical-correctness and honesty release. Reported confidence intervals CHANGE BY DESIGN relative to 0.2.x: wider where oracle-calibration uncertainty was understated (OUA jackknife x K bug), narrower where Monte Carlo variance was double-counted, and several silent fallbacks now fail loudly. See the entries below and PRs #10-#14.

- **Documentation sweep — make every snippet run and every claim match the code**: fixed broken imports/attributes in README snippets (`cluster_bootstrap_direct_with_refit`/`make_calibrator_factory` module path, `TransportDiagnostics.delta_ci` instead of nonexistent `ci_lower`/`ci_upper`, `analyze_dataset` usage example that showed a nonexistent `cluster_id_field`/`var_cluster_robust` API, `fit_and_estimate(compute_diagnostics=...)`, `result.robust_standard_errors`, three snippets with syntax errors); aligned claims with code (two-stage calibration is mean-preserving by construction — no "recentering" step; SimcalConfig docstring defaults include_baseline=False/baseline_shrink=0.0; K=2 SIMCal candidates by default; oracle-label guidance standardized to the paper's 5-25% everywhere); estimators README now recommends Direct mode first with StackedDR as the DR-mode default, matching the paper and root README; root README's tests badge points at the live CI workflow, the not-yet-committed Inspect AI bridge is no longer advertised, and the forest-plot caption scopes its "valid confidence intervals" claim to the checks CJE runs; `analyze_dataset` docstring metadata keys corrected; ghost estimators (`oc-dr-cpo`, `tr-cpo`, `tr-cpo-e`) removed from the interface's mode-inference lists (the factory cannot build them); logprob fields documented as IPS/DR-only (Direct mode needs none); assorted docstring fixes (get_fold example values, detect_analysis_mode 3-tuple, Hellinger relation H=sqrt(1-A), nonexistent paper-section citation, `_harmonize_if_scales` honest no-op docstring); CLI estimate line labels its ± as 1 SE.
- **Add a documentation-snippet test** (`cje/tests/test_doc_snippets.py`): every ```python block in the repo READMEs must compile, and every import in every snippet must resolve (optional-extra imports skip with a note). This immediately caught six additional broken snippets beyond the review's findings.
- **Expose refuse_unreliable end-to-end; stop the CLI crowning gate-flagged policies**: diagnostics/README.md claimed estimators "refuse estimation (return NaN)" automatically while `refuse_unreliable` defaulted to False and the DR/stacked factory builders silently dropped the key. The flag now flows from `analyze_dataset(estimator_config={"refuse_unreliable": True})` to CalibratedIPS, raw-ips, and every DR estimator's internal `ips_estimator` (dr-cpo/mrdr/tmle and stacked-dr components); the README's "Diagnostic Gates" section and design principle #3 now state the real default (warn loudly, estimate anyway; NaN only on request) and document that the Hill tail index is a `weight_status` input, not a NaN-refusal gate. Per-policy gate outcomes are recorded in `result.metadata["reliability_gates"]` (`flagged`/`refused`/`reasons`) regardless of the flag — written by `CalibratedIPS.estimate()` and propagated to the DR family and stacked-dr by the release-respin entry below. The CLI previously printed "🏆 Best policy" from the raw argmax even when that policy was flagged UNRELIABLE (on the bundled arena sample the adversarial 'unhelpful' policy won the trophy with the warning scrolled away): `cje analyze` now consults the reliability gates and per-policy statuses — a flagged argmax prints "⚠️ Best by point estimate: X (UNRELIABLE — see diagnostics)" followed by the best RELIABLE policy when one exists, and all-NaN results say so instead of crowning anything.
- **Release respin — propagate the reliability gates to the DR family and StackedDR; two data-handling fixes**: the gate outcomes above were written only by `CalibratedIPS.estimate()`, so DR results dropped `reliability_gates` and StackedDR built its diagnostics without `status_per_policy` — leaving the CLI's demote-unreliable logic inert on the CLI's own default estimator (verified: a stacked-dr run on a synthetic dataset whose catastrophic-overlap policy wins the argmax crowned it with no warning). DR-CPO/TMLE/MRDR now copy `reliability_gates`, `ttc_diagnostics`, and `boundary_cards` from their internal IPS result into their own metadata; StackedDR copies them from its dr-cpo component (the components share the sampler and weights) and populates `status_per_policy` as the worst per-policy status across components, so the flagged-argmax demotion fires on every estimator surface. Also: fresh-draw calibration pairs are keyed on response identity (`fresh_draws:<policy>:draw<i>`) instead of the constant `fresh_draws` source, so binary/rubric scores no longer collapse distinct policies' draws for one prompt into a single pair (conflict detection still compares source families); and a blank or trailing line in a fresh-draws JSONL is skipped (matching the logged-data loader) instead of crashing the loud parse-error path.
- **Implement the paper's judge-space overlap gate and wire in the coverage badge**: the library graded overlap only via action-space `E[sqrt(w)]` ("good" at 0.50) while the paper gates on the binned judge-space Bhattacharyya affinity A_B >= 0.85; the declared `aessf_sigmaS`/`bc_sigmaS` fields were never populated (CF-bits vestiges) and the README claimed nonexistent "σ(S) floors". New `bhattacharyya_judge_space(...)` computes the paper's binned A_B (fresh-draw S vs logged S when draws are available — DR does this automatically — else the raw-weight-weighted vs unweighted logged-S histogram); `CalibratedIPS` populates `IPSDiagnostics.bc_sigmaS_per_policy` and folds it into statuses via the canonical gates (>= 0.85 pass, < 0.5 severe); `OverlapMetrics.bc_sigmaS`/`aessf_sigmaS` (= A_B², the σ(S) structural ESS ceiling) are populated when judge scores are passed; `hellinger_affinity` is documented as the distinct action-space quantity (unchanged, not silently redefined; the unused `aessf_sigmaS_lcb` field was removed). Separately, `boundary_card` (the paper's REFUSE-LEVEL coverage badge, out-of-range >= 5%) had zero callers and was unexported while `CJEDiagnostics` hardcoded `coverage_risk="low"` so `refuse_level_claims` could never fire: `boundary_card`/`BoundaryCard` are now exported, `CalibratedIPS` computes a per-policy card against the oracle calibration S-range (attached to `diagnostics.boundary_cards` + `result.metadata`, copied into DR diagnostics, loud warning on REFUSE-LEVEL), and `CJEDiagnostics.from_ips_diagnostics` derives `coverage_risk` from the cards (REFUSE-LEVEL => "critical" => `refuse_level_claims=True`; no cards => "unknown", never a hardcoded "low", and `can_make_level_claims` stays False until coverage is actually checked).
- **Compute the paper's TTC gate during estimation**: the paper's headline diagnostic (Target-Typicality Coverage; gate TTC >= 0.70 before trusting logged data) existed as `compute_ttc` but had zero callers in the pipeline AND implemented a different statistic (total sequence surprisal at 0.8 target mass instead of the paper's mean per-token surprisal at q_0.9 — total NLL turns the "typical" region into a short-response region). `compute_ttc`/`compute_cle_diagnostics` now default to `target_typical_mass=0.9` and accept `token_counts` for the per-token construction; `CalibratedIPS` computes TTC per policy at estimation time (using a `response_token_count` metadata field when present, else response length in characters as a documented proxy, recorded in `result.metadata["ttc_diagnostics"]`), populates the new `IPSDiagnostics.ttc_per_policy` field (copied into `DRDiagnostics`, along with the previously dropped per-policy status/tail/Hellinger fields), folds TTC into `weight_status`/`status_per_policy` via the canonical gates (WARNING < 0.70, CRITICAL < 0.30), warns loudly when the gate fails, and surfaces "Worst TTC" in `summary()`, `validate()`, and CSV export. Fixed the `CLEDiagnostics` docstring claiming "alpha ≈ 0.8 by construction" and the mislabeling of `E[sqrt(w)]` as a "surrogate space" quantity.
- **One canonical ESS ladder + honest tail-index failures**: ESS thresholds were mutually inconsistent across six surfaces (weights.py 0.01/0.1, the CalibratedIPS refusal gate 0.30, display.py 0.5/0.2, weight dashboards 0.5/0.2/0.1, and two contradictory ladders inside diagnostics/README.md) — a policy at ESS 0.25 could read GOOD, "UNRELIABLE ESTIMATE", and "Good — usable with caution" in the same run. All surfaces now import one ladder from the new `cje/diagnostics/gates.py` (`ess_status`: >= 0.30 GOOD — the paper's ship-gate — / >= 0.10 WARNING / else CRITICAL), which also centralizes the TTC, judge-space Bhattacharyya, tail-index, and coverage-badge thresholds. `hill_tail_index` now returns NaN on estimation failure (too few samples, >~95% exact-zero weights, numerical issues) instead of +inf, which every downstream check previously read as the healthiest possible tail; +inf is reserved for genuinely uniform tails. NaN tails are treated as *unknown* downstream (`tail_status` escalates to WARNING when ESS is also below the ship-gate; `worst_tail_index` no longer launders failures through `min(..., default=inf)`; `validate()` reports the failure; `analyze_diagnostics` counts `tail_unknown` separately from `heavy_tail`; display helper renders "n/a"). Regression tests in `cje/tests/test_refusal_gates.py`.
- **Keep every oracle (judge, oracle) pair when combining sources**: `_combine_oracle_sources` used to dedupe by prompt_id alone, so K policies' fresh-draw labels for the same prompt collapsed to the last-iterated policy's pair while `n_from_fresh` counted every overwrite. Oracle labels attach to responses, not prompts: all pairs now enter calibration (only true duplicates — same source + prompt + identical judge & oracle values — are deduped), per-source counts reflect what actually enters, the cross-source conflict warning fires unconditionally, and the obsolete `priority_order` metadata key is gone. Also fixed `CalibratedIPS.fit`'s duplicate-prompt_id branch, where an unbound `ds_idx` raised an `UnboundLocalError` that a blanket except swallowed — silently skipping the fold-based OOF fallback it was supposed to fall through to.
- **Fix calibration misalignment and fabricated fallbacks**: integer (index) `oracle_mask` inputs to `JudgeCalibrator.fit_transform`/`fit_cv` are now argsorted with oracle labels reordered identically (unsorted indices used to silently misalign every (score, label) pair; verified RMSE 0.032 → 0.302) and duplicate indices are rejected; `predict_oof` raises for fold ids without a fitted model instead of zero-filling predictions; `FlexibleCalibrator` raises `RuntimeError` instead of fabricating constant-0.5 or mean-of-judge-score rewards when no models are fitted; and monotone calibration warns loudly when the fitted mapping collapses to a constant while oracle labels vary (anti-correlated/inverted judge scale), which previously erased all policy differences silently.
- **Harden data loading and validation against silent corruption**: `Sample` now rejects NaN/±inf logprobs (previously they passed validation and silently zeroed or exploded importance weights; use `None` for missing); `load_fresh_draws_auto` propagates per-record parse errors with file/line context instead of swallowing them and misreporting `FileNotFoundError`; `DatasetLoader` no longer hash-replaces falsy-but-valid prompt_ids (0, "") and coerces integer ids via `str()` (matching fresh-draw loading so joins work), replaces `print()` skips with counted `logger.warning`s and raises when every record was skipped; `validate_cje_data` scans the reward field over the same window as judge/oracle checks (was: only `data[0]`) and reports the missing fraction; `add_rewards_to_existing_data` keys the judge-score collection and reward join on record index so duplicate prompt_ids each get f(their own judge score) instead of all inheriting the last record's reward.
- **Fail loudly on silent interface downgrades**: `analyze_dataset` now raises when `fresh_draws_data` is combined with `logged_data_path` (the in-memory draws were silently ignored — no DR, metadata claimed `has_fresh_draws=False`); auto mode raises `FileNotFoundError` when `fresh_draws_dir` does not exist instead of silently downgrading DR→IPS; and zero-oracle Direct runs emit a prominent warning and are labeled `method="naive_direct"` instead of `"calibrated_direct"` (uncalibrated judge-score means were previously indistinguishable from calibrated results).
- **Fix CalibratedIPS influence function understating the SE ~2x**: (a) the Hajek ratio IF included an extra `−ψ(w−mean_w)` term that double-counted the ratio-denominator correction (the delta method gives `φ = w(R−ψ)/mean_w`); (b) at 100% oracle coverage the IF was built from smoothed OOF calibrator predictions while the estimate consumed raw oracle labels, deleting the reward-noise term from the variance. Discovered by the new Monte Carlo coverage harness (reported-SE/empirical-SD ratio was 0.44-0.63 at full oracle coverage; now ~1.0).
- **Add a Monte Carlo ground-truth coverage harness** (`cje/tests/test_mc_coverage.py`): synthetic DGP with a known policy value; fast CI layer asserts point-estimate bias and reported-SE-vs-empirical-SD bands for calibrated-ips and dr-cpo (catching any resurgence of the OUA ×K, MC double-count, or IF-formula bugs), plus a slow 300-replicate 95% CI coverage layer across direct/calibrated-ips/dr-cpo/tmle/stacked-dr.
- **Unify DR-family standard errors and stop double-counting Monte Carlo variance**:
  - DR-CPO no longer adds a separate fresh-draw MC variance term to its SE. The influence functions are built from per-prompt means of the fresh-draw predictions, so the (cluster-robust) IF variance already contains the within-prompt MC noise; re-adding `mc_var` in quadrature inflated SEs by up to ~sqrt(2)x when M=1. MC numbers remain available as diagnostics in `metadata["mc_variance_diagnostics"]` (per-prompt variances now use ddof=1). Reported CIs narrow by design.
  - TMLE and MRDR previously reported naive IID SEs (`np.std/sqrt(n)`) with no cluster-robust correction and no `degrees_of_freedom` metadata (so their CIs silently used z instead of t) — a different basis from DR-CPO. All three DR estimators now share one SE helper (`DREstimator._compute_policy_se`): cluster-robust IF SE on outcome folds + delete-one-fold oracle-jackknife variance (added exactly once; `_apply_oua_jackknife` no longer re-adds it), with t-based df metadata and a per-policy SE decomposition in `metadata["se_components"]`.
  - StackedDR: removed the MC-aware rank-1 term from the weight objective and the `mc_var` composition from the stacked SE. Only dr-cpo ever reported MC diagnostics, so the penalty asymmetrically inflated dr-cpo's diagonal in the weight QP and tilted weights toward tmle/mrdr for reasons unrelated to efficiency; the stacked IF already contains the MC noise. `include_mc_in_objective`/`mc_lambda` kwargs are accepted with a `DeprecationWarning` and ignored. Aggregated MC variance is diagnostics-only.
  - `compute_robust_inference` (public diagnostics API): bootstrap/cluster CIs were centered at ~0 (mean-centered influence functions, returned unshifted) and never contained the estimate; they are now re-centered on each point estimate. The raw `data=` path used a constant placeholder statistic (SE ~1e-16, zero-width CI, p=0.0) and now raises `ValueError` directing users to pass `influence_functions`.
  - Regression tests in `cje/tests/test_inference_unification.py` pin: exact SE composition (IF + oracle only) with MC as metadata; all three DR estimators routing through the shared helper with t-based df; cluster-robust (not naive) IF SEs; equal stacking weights for identical IFs regardless of reported MC diagnostics; and CI-contains-estimate for every `compute_robust_inference` method.
- **Fix OUA jackknife variance understated by a factor of K** in `base_estimator._apply_oua_jackknife` and `stacking._apply_stacked_oua` (mean over folds instead of the delete-one-fold jackknife sum, paper Alg. 6). With the default K=5, the calibration-uncertainty contribution to reported standard errors was ~2.24x too small for calibrated-ips, calibrated direct (jackknife path), TMLE, MRDR, and stacked-dr. All OUA sites now share one `oracle_jackknife_variance()` helper. Reported CIs widen by design.
- **Fix `calibration_data_path` replacing the evaluation dataset**: in IPS/DR mode the estimator was built from the calibration (or combined-oracle) pool — whose fabricated logprobs made every policy's estimate identical with a perfect ESS — instead of the logged data. The calibration pool is now used only to fit the calibrator, whose rewards are applied to the logged evaluation dataset before estimation. Regression tests pin distinct per-policy estimates and logged-length influence functions.
- **Fix TMLE collapsing to the untargeted plug-in**: the fluctuation was solved on logged data but never propagated to fresh draws, so the solved score equation cancelled the IPS correction exactly and `TMLEEstimator` returned the direct-method plug-in with a DR-type SE (no double robustness). The targeting step now uses a score-indexed clever covariate h(S)=E[w|S] (fit per fold; exact for S-calibrated weights) and applies the targeted outcome model to fresh draws per draw. Regression tests pin double robustness under deliberate outcome-model misspecification and first-order agreement with DR-CPO.
- **Fix OUA jackknife predictions for two-stage calibrators**: leave-one-fold reward predictions in `CalibratedIPS`, `DREstimator`, and `CalibratedDirectEstimator` fed raw judge scores to per-fold isotonic models that expect the ECDF rank index, corrupting jackknife replicates whenever `two_stage` calibration was active (auto-selectable by default; always active with covariates). All jackknife paths now route through `reward_calibrator.predict_oof(...)`, and `CalibratedIPS` gained covariate support in its jackknife.
- Fix the README and interface-README Quick Start examples, which crashed verbatim because they provided fewer than the 10 oracle labels required by 5-fold cross-fitted calibration; document the 10-label minimum next to the labeling guidance.
- Reduce calibration folds gracefully (with a warning) in Direct mode when 4-9 oracle labels are available instead of failing; below 4 labels, the calibration error now says exactly how to fix it.
- Handle degenerate fold assignments in FlexibleCalibrator (all oracle samples hashed into one fold) by falling back to fitting on all oracle samples for that fold.
- Reduce full-data calibration folds for small oracle slices in cluster-bootstrap inference so Direct-mode point estimates no longer come back NaN with fewer than 10 labels.

## 0.2.25

- Remove the experimental multi-policy EIF implementation and standardize Direct-mode bootstrap on per-policy residual correction.
- Keep a temporary compatibility shim so legacy `use_multipolicy_eif=False` configs warn and are ignored, while `True` now fails fast with a clear error.
- Remove stale docs and tests for the retired multi-policy EIF path.

## 0.2.24

- Speed up planning variance fitting by using a lighter internal bootstrap during repeated measurement loops.
- Fix `fit_variance_model(...)` so `oracle_fraction_grid` actually controls the calibration grid.
- Make notebook tests execute the local checkout rather than reinstalling a published package inside the test kernel.
- Validate the planning workflow, planning notebook, simulation planning suite, and packaged wheel before release prep.

## 0.2.x before 0.2.24 (historical note)

Releases v0.2.0 through v0.2.23 predate this changelog and have no entries; see git tags and the GitHub release history.
